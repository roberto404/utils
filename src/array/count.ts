/**
 * Function returns the length of array.
 *
 * @since 3.2.0
 * @static
 * @memberof array
 * @param  {array} array inspected array
 * @return {int}
 * @example
 *
 * count([1,3,2]);
 * // => 3
 */
const count = (array: Array<number>): number => array.length;

export default count;
